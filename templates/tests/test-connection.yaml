apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-mysql-ase.fullname" . }}-test-connection"
  labels:
{{ include "helm-mysql-ase.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "helm-mysql-ase.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
